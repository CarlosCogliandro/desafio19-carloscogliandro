<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.0/dist/css/bootstrap.min.css"
        integrity="sha384-B0vP5xmATw1+K9KRQjQERJvTumQW0nPEzvF6L/Z6nronJ3oUOFUFpCjEUQouq2+l" crossorigin="anonymous">
    <title>Lista de Productos</title>
</head>
<body>
    <h2 class="text-primary m-3 me-auto text-center">Vista de Productos</h2>
    <% if(products.length > 0){ %>
        <div class="table-responsive p-3">
            <table class="table table-dark">
                <tr>
                    <th class="text-center"><b>Codigo</b></th> 
                    <th class="text-center"><b>Titulo</b></th> 
                    <th class="text-center"><b>Thumbnail</b></th> 
                    <th class="text-center"><b>descripcion</b></th> 
                    <th class="text-center"><b>Precio</b></th> 
                </tr>
                    <% for (const product of products) { %>
                <tr> 
                    <td class="text-center text-light"><%=product.code%></td>
                    <td class="text-center text-light"><%=product.title%></td>
                    <td class="text-center text-light"><%=product.image%></td>
                    <td class="text-center text-light"><%=product.description%></td>
                    <td class="text-center text-light">$ <%=product.price%></td>    
                </tr>
                <%};%>
            </table>
        </div>
    <% } else{ %>
        <h3 class="alert alert-warning m-5">No se encontraron productos</h1>
    <% } %>
    <a href="./home"><button class="btn btn-danger m-5">Volver</button></a>
</body>
</html>